// Package grpc implements the gRPC transport handler layer.
//
// It defines the root handler type used to expose service-layer operations
// through gRPC endpoints and middleware chains.
package grpc
